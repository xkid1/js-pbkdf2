import { describe, expect, test, it } from '@jest/globals';

describe('PBKDF2JS', () => {
    // it('should be defined', () => {
    //     expect(pbkdf2js).toBeDefined();
    // }),
    //     it('decrypt should be a function', () => {
    //         // expect(typeof pbkdf2js.decrypt).toBe('function');
    //     }),
    //     it('encrypt should be a function', () => {
    //         // expect(typeof pbkdf2js.encrypt).toBe('function');
    //     });
    // it('should return a PBKDF2JS instance', () => {
    //     expect(pbkdf2js() instanceof pbkdf2js).toBe(true);
    // });
});
